language: rust
rust:
  - stable
  - beta
  - nightly
cache:
  directories:
    - /home/travis/.cargo
install:
  - rustup toolchain add nightly
  - cargo +nightly install --force clippy; true
script:
  - cargo +nightly clippy -- -D warnings
  - cargo test
